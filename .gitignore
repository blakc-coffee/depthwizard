# Claude — never commit any of this
.claude/
CLAUDE.md
ONBOARDING.md

# Personal project preferences (create manually alongside CLAUDE.md)
CLAUDE.local.md

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
out/
.next/
.nuxt/

# Rust
target/

# Python
.venv/
__pycache__/
*.pyc

# ML model weights/checkpoints
ml/models/

# Derived sanity-test imagery (regenerable, keeps binary bloat out of git)
ml/depth/samples/
# Cargo.lock: commit it for binaries, gitignore it for libraries — see https://doc.rust-lang.org/cargo/faq.html#is-it-advisable-to-commit-cargo-lock-and-when
data/
