[pytest]
; Run from backend/ (e.g. `cd backend && pytest`). `.` makes `app.*`
; importable, `..` makes `integration.*` importable — neither package uses
; __init__.py (implicit namespace packages), matching the ml/ convention.
pythonpath = . ..
