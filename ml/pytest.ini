[pytest]
; Run from ml/ (e.g. `cd ml && pytest`). `.` makes flat modules (utils.*,
; depth.*, data.*) importable without __init__.py, matching every existing
; ml/ module's import style (see integration/pipeline_runner.py's docstring).
pythonpath = .
